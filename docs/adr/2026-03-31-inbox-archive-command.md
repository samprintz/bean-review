# Inbox Archive Command

After reviewing beancount transactions, import files (CSV, PDF, …) that have
been fully processed should be moved out of the inbox — typically into an
archive directory — without requiring the user to leave the TUI.

## Decision

Add an archive action to the Inbox Screen, mirroring the import action.
